/**
 * AdminSidebarConfig Model (Native MongoDB Driver)
 */

import { createNativeModel } from './_baseModel.js';

const AdminSidebarConfig = createNativeModel('adminsidebarconfigs', 'paid');

export default AdminSidebarConfig;
